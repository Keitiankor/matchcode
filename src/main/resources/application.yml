server:
  port: 8765
  servlet:
    session:
      tracking-modes: cookie

spring:
  profiles:
    default: dev

  mvc:
    view:
      prefix: /resources/templates
      suffix: .html
    hiddenmethod:
      filter:
        enabled: true

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/test_hyem?serverTimezone=Asia/Seoul
    username: root
    password: mario


  #  data:
  #    mongodb:
  #      host: keitian.moe
  #      port: 27017
  #      username: study
  #      password: 1234
  #      database: connectiontest
  #      authentication-database: admin

  jpa:
    open-in-view: true
    database: mysql
    show-sql: true
    hibernate:
      ddl-auto: update # ddl 자동작성여부(update)
      naming:
        physical-strategy: org.hibernate.boot.model.naming.CamelCaseToUnderscoresNamingStrategy

  logging:
    level:
      org.springframework: DEBUG
      org.thymeleaf: DEBUG
  mail:
    host: smtp.gmail.com
    port: 587
    username: matchcode.multi@gmail.com
    password: xevzkrffbzrzojwa
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
---
server:
  port: 8765

spring:
  config:
    activate:
      on-profile: dev

  jpa:
    hibernate:
      ddl-auto: update
---
server:
  port: 9999

spring:
  config:
    activate:
      on-profile: prod

  jpa:
    hibernate:
      ddl-auto: validate
